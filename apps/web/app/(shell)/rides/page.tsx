import { WorkspacePage } from "@/components/WorkspacePage";

export default function RidesPage() {
  return (
    <WorkspacePage
      kicker="RIDE CENTER"
      title="Where to next?"
      description="Plan around terrain, weather, and machine hours."
    />
  );
}
