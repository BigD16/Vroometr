# FastAPI backend

Not wired up yet (task 0.3).

When it exists:

- `routes/` — HTTP only
- `services/` — business rules
- `repositories/` — database

If you want to change an API endpoint, start in `routes/`. If you want to change a rule, start in `services/`.
